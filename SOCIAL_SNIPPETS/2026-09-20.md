# Social Snippets — 2026-09-20 (Run #5)

## LinkedIn
Sporewick finally has real art: a 10-piece hand-drawn spore icon set
(base, hybrid, and "refined" tiers, plus a mystery fallback) replacing
four runs' worth of plain text buttons, and a matching app icon. Also
shipped a real store-listing screenshot at the actual required
resolution. The best find of the session wasn't the art though — it was
a bug: my new icons loaded fine in my dev session but would have
silently failed on a totally fresh checkout, because Godot's texture
import cache is (correctly) gitignored and nothing was regenerating it.
The automated gameplay tests all still would have passed while the game
rendered with no icons at all — a good reminder that "tests are green"
and "it actually looks right" are different claims. Fixed the tooling,
re-verified from a simulated clean clone. 10 days left until the
Shipaton deadline now, and the store-account/RevenueCat setup that needs
a human hasn't started yet — flagging that loudly this time.

## Twitter/X
Gave Sporewick real mushroom art today (10 hand-drawn spore icons + a
matching app icon) instead of 4 runs of plain text buttons. Also found
a sneaky bug: new art assets would've silently failed to load on a
fresh checkout because the import cache wasn't being rebuilt. Tests
were green the whole time. Fixed. #Shipaton #BuildInPublic

## YouTube community post / short caption
Before: "Ember Cap ready! (tap to harvest)" in plain text. After: an
actual little mushroom icon. Small thing, makes the whole plot grid
finally look like a game. #Shipaton #BuildInPublic

(Pairs with `game/sporewick/store_listing/store_screenshot_1179x2556.png`
— the real store-listing screenshot captured this run.)
